#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @q: the int value to check
 * Return: the absolute value of int
 */
int _abs(int q)
{
if (q >= 0)
{
return (q);
}
else
return (q * -1);
}
