#include <unistd.h>
#include <stdlib.h>

/**
 *printf - entry point
 *
 *return - always return 0
 */

int printf(const char *format, ...)
{
write(1, "9 8 10 24 75 9", 14);
exit(EXIT_SUCCESS);
return 0;
}
